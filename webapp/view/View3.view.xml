<mvc:View xmlns:form="sap.ui.layout.form"
    controllerName="demofiori.project1.controller.View3"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core">
    
    <Page id="page3"
            title="{i18n>webapp.title}" >
            <!-- Express Binding -->
             <Label text="Name"/>
            <Input value="{uiModel>/name}" />
            <Button text="save" enabled="{= ${uiModel>/name}.length>0 }"></Button>
            <Text id="textName" text="{uiModel>/name}"></Text>

            <!-- <HBox >
                <Text text="{uiModel>/amount}"></Text>
                <GenericTag 
                    visible="{= !!${uiModel>/amount} }"
                    text="{= ${uiModel>/amount}>1000 ? 'Success' : 'Warning' }"
                    status="{= ${uiModel>/amount}>1000 ? 'Success' : 'Warning' }" />
            </HBox> -->

            <!-- Fragment -->
            <!-- <VBox id="formcontainer"> -->
                <!-- <core:Fragment 
                    id="form" 
                    fragmentName="demofiori.project1.fragment.form"
                    type="XML"/> -->
            <!-- </VBox> -->

            <Button  text="open" press="openDialog">
            </Button>
    </Page>
</mvc:View>